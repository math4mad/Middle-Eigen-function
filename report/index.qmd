---
title: "Middle Eigen Function"
subtitle: "消融实验：权重矩阵的“中间特征值”是否赋予模型泛化能力？"
author:
  - name: "math4mad（假设 · 协议设计 · 数据与硬件）"
    attributes:
      role: [Conceptualization, Methodology, Resources, Supervision]
  - name: "pi coding agent · 模型 qwen3.8-flash（实现 · 实验 · 统计 · 报告）"
    attributes:
      role: [Software, Validation, Formal analysis, Writing - original draft]
author_notes: |
  分工：人类提出假设、设计协议、提供数据与硬件；AI 代理（pi 编码代理）实现脚本、运行实验、
  做配对统计与显著性检验、撰写报告页与任务书。所有数字均由 `scripts/` 里的脚本从
  `outputs/` 的原始结果自动渲染，可复算。
date: last-modified
---

## 动机 (Motivation)

> 如果把生物神经网络与人工神经网络做类比：模型参数矩阵的 SVD 分解可以对应为
> “骨骼（最大奇异值）、血液与组织（中间奇异值）”。训练过程类似树突增粗（强化已有方向）
> 与突触密度增加（建立大量弱连接）。因此我们假设：**ANN 的泛化能力主要来源于权重矩阵
> 中分布广泛的中间特征值（松散谱），而不是极少数最大的特征值（刚性骨架）。**

## 实验设计：三步消融法 {#design}

:::note
**三步消融**

- **A 组 · 刚性骨架**：保留最大的前 10% 奇异值，其余置 0
- **B 组 · 松散谱**：去掉最大 10% 与最小 10%，保留中间 80%
- **C 组 · 随机丢弃**：随机保留 80%（对照：数量 vs 位置）
- **基线**：不做任何截断的预训练权重
:::

对 `bert-base-uncased` 全部 12 层 MLP 的 `c_proj`（HF 实现中的 `output.dense`，
形状 `768×3072`）逐矩阵做 SVD，按上表规则将奇异值置 0 后重构权重；
四个模型用完全相同的超参数在 **GLUE/RTE 的 10% 子集（249 条）** 上微调 8 个 epoch，
在 RTE dev（277 条）上评估。

## 全项目一页看全（Stage 1 → 17）

| 阶段 | 想回答的问题 | 一句话结果 | 判读 |
|---|---|---|---|
| **1 · BERT 四组消融** | 留最大 10% / 留中间 80% / **随机**留 80%，谁泛化好？ | **C 随机 0.610** > B 中间 0.552 > 基线 0.542 > A 骨架 0.534（全参微调） | 中间谱假说的最初证据；**而且第一名是"随机留"** —— 数量 > 位置，从第一天就在那儿 |
| **2 · 跨架构复验** | 换个模型还成立吗？ | RoBERTa/ELECTRA/DistilBERT 方向一致但幅度不一 | 部分复现 |
| **3 · 谱拼接杂交** | 中段谱能跨模型移植吗？ | 可移植性有限，尺寸/基座错配即失效 | 否 |
| **5 · Qwen 深度验证** | 是"位置"还是"能量"？ | **等能量后位置残差只剩 +0.015（噪声内）**；新发现 L6（切后 6 层）0.585 | 假说需分微调方式讨论 |
| **6 · 垃圾 DNA 扩层** | 复制层"拼大模型"能长出新能力吗？ | 参数变大、能力没长出来 | 否 |
| **7 · 跨尺寸填充** | 零填充打通尺寸后杂交呢？ | 旋转/能量/纯噪声三重对照下 ≈ 噪声 | 否 |
| **9 · L6 稳健性检验** | L6 的 +0.058 稳吗？ | 4 seed 均值 **+0.021（p=0.28）**；合并 6 配对 +0.026；倒 U 成立；1.5B 只剩 +0.014；30% 数据不缩小 | **幅度降级为噪声带** |
| **10 · 在线 SVD × 垃圾注入** | 训练中能不能"闻出"污染？ | `rel` ρ=+0.88、`mid_E` ρ=+0.83 单调；**垃圾写在中带不写尾部**；3σ 报警太晚 | **监测可行、报警尚不可行** |
| **11–14 · 坐标之争** | σ 的大小序 = 功能序吗？ | 保谱重排 **Δ=−0.004**（噪声内）；τ(σ序,功能序) **−0.22 / −0.12**；`ATOM64`≈`RAND64`；`ATOMfull`(1/61 参数) 0.578 vs 0.556 | **大小序粗尺度够用、细尺度失效** |
| **12 · 定向删除** | 污染能不能定点删掉？ | 三组全 ≤0，**按污染指纹删最差**（−0.032） | **撤回删除式手术**（待 Stage 16 等能量复验） |
| **15–17 · 下一步** | 正向结果复验 / 等能量判决 / 报警做实 | 任务书见 [`stage/`](https://github.com/math4mad/Middle-Eigen-function/tree/multi-model/stage) | 计划中 |

:::{.callout-tip title="一句话总结"}
我们本来想用"奇异值大小"给模型零件排座次、再按座次做手术；
实测是：**音量序在粗看时管用、细看时失效** ——
真正决定小样本微调效果的是"**给学习过程多大自由度**"，而不是"切掉了第几大的那些"。
名词不记得 → [术语页](notes/glossary.qmd)。
:::

## 贡献与可复现性 (Contributions & Reproducibility)

:::{.callout-note title="作者分工"}
- **math4mad** — 假设提出（"泛化能力住在松散谱里"）、实验协议设计（三步消融 / 等能量对照 / 剂量-反应）、
  生物类比与判据、数据与硬件（M1 Pro 16 核 / 32GB，全部实验在本机跑）。
- **pi coding agent（模型 qwen3.8-flash）** — 脚本实现、实验运行与断点续跑、配对统计
  （t 检验 / bootstrap CI / Spearman / Kendall τ / Cohen's d）、报告页与任务书撰写、
  以及主动否证自己前一轮结论的那几次（stage9 降级 +0.058、stage10 否证"垃圾住尾部"、
  stage12 撤回删除式手术）。
:::

**可复算**：本报告每一张表里的每个数字都由 `scripts/` 下的脚本从 `outputs/` 的原始 JSON 渲染，
页面本身是 Quarto + Jupyter 执行产物（`report/_freeze/` 存有执行缓存）。
任务书与判据按阶段存放在仓库 `stage/`（含未跑的 `stage15–17.md`）。

## 先读这里

**如果名词记不住了**：[**术语页 Glossary**](notes/glossary.qmd) —— 每个名词一句人话 +
它在本项目里具体指哪个东西（SVD / 奇异值 / rank-1 原子 / 谱 / LoRA / ΔW / 等能量对照 /
TAIL-MID-FRONT / 保谱重排 / 损伤探针 / Kendall τ / 噪声带 ±0.03 / padding mask 的坑 …… 全在里面）。

**一句话结论**：我们想用"奇异值大小"给模型零件排座次、再按座次做手术；
实测是 —— **音量序在粗看时管用、细看时失效**，真正决定微调效果的是"给学习多大范围"，
而不是"切掉了第几大的那些"。

## 阅读顺序

1. [**Ablation Note (BERT)**](notes/eigen-ablation.qmd) —— 首轮完整实验记录：谱分布、四组训练曲线、准确率/泛化误差表、"入侵维度"分析。
2. [**Multi-Model**](notes/multi-model.qmd) —— 阶段二：RoBERTa/ELECTRA/DistilBERT/Qwen2.5-0.5B(LoRA) 跨架构复验 + 等能量对照。
3. [**Hybrid Splice**](notes/hybrid.qmd) —— 阶段三：跨模型谱拼接杂交（top 段来自 X + middle 段来自 Y），检验中段谱的可移植性。
4. [**Qwen Deep-Dive**](notes/qwen-deep.qmd) —— 阶段五：Qwen2.5-0.5B+LoRA 深度验证（等能量对照/剂量曲线/分层定位），含对早间结论的修正。
5. [**Junk-DNA Expansion**](notes/junk-dna.qmd) —— 阶段六：门控层复制把 109M 扩到 195M，"补齐参数拼大模型"能否产生新能力？
6. [**Cross-Size Pad**](notes/cross-size.qmd) —— 阶段七：零填充打通尺寸差异，做"跨公司跨尺寸"杂交 —— 含随机旋转/能量配平/纯噪声三重对照。
7. [**L6 Robustness (Stage 9)**](notes/l6-robustness.qmd) —— "切深层 top 谱 = 删有害记忆"的四重检验（多 seed / 边界 / 1.5B / 30% 数据）：方向成立，幅度从 +0.058 修正为 +0.02~+0.03。
8. [**Online SVD × Junk Injection (Stage 10)**](notes/online-svd.qmd) —— 把 ΔW 的谱（有效秩 / 入侵分位 / 旋转角）做成逐 step 在线监测，用 RTE test 伪标注做主动垃圾注入：垃圾写在**中带**、不在尾部。
9. [**Coordinates: σ-order vs Function (Stage 11–14)**](notes/atom-coordinates.qmd) —— 随机子空间训练、**保谱 Shuffle-Rebuild**、布洛卡式单方向损伤（Kendall τ）、rank-1 原子重参数化：σ 序在粗尺度有效、细尺度失效。

全部实验代码见仓库 [`scripts/`](https://github.com/math4mad/Middle-Eigen-function) 目录。
