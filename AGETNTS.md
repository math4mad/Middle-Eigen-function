好的，没问题。根据我们刚才的讨论，我将实验设计整理成了一个 AGENTS.md 文件，你可以直接将其放入项目根目录，作为与AI编码助手协作的共享上下文。

这个文件的目标是：指导AI助手执行一个严谨的消融实验，以验证“权重矩阵中间特征值赋予模型泛化能力”的假设。

AGENTS.md
项目目标
验证预训练语言模型（LLM）中，权重矩阵的中间奇异值（即“松散谱”）对模型在小样本微调后泛化能力的贡献。

核心假设
模型的泛化能力主要源于权重矩阵中分布广泛的中间特征值（松散谱），而非极少数最大的特征值（刚性骨架）。
保留中间特征值进行微调，应能获得比保留大特征值更好的泛化性能。
实验设计：三步消融法
1. 准备基线模型
加载一个标准预训练模型（如 GPT-2 或 BERT-base）。
对关键层（如 mlp.c_proj）的权重矩阵进行SVD分解，得到其奇异值谱。
2. 构造三个对照实验组
对同一个权重矩阵，分别进行以下操作，得到三个修改后的模型：

A组：刚性骨架版

操作：保留奇异值中排名前10%（最大的），将剩余90%全部置为0。
预期：保留最强方向，丢弃所有“松散”方向。
B组：松散谱版

操作：去掉最大10%和最小10% 的奇异值，只保留中间80% 不变。
预期：保留最广泛的“松散”方向，丢弃最强和最弱的方向。
C组：随机丢弃版

操作：从所有奇异值中随机选择80% 保持不变，其余20%置为0。
预期：作为控制组，检验是否仅仅是保留足够多的奇异值数量就有效，而与位置无关。
3. 微调与评估
数据集：从 GLUE/RTE 任务中选取10%的样本作为小样本训练集。
微调：对上述三个修改后的模型（A、B、C）以及原始未修改的基线模型，使用完全相同的超参数进行微调。
评估：
在 RTE 测试集上评估准确率。
记录训练集上的损失下降速度与测试集性能的差距（泛化误差）。
预期结果与解读
若B组（松散谱版）性能最好：直接验证了核心假设，中间特征值是泛化能力的主要来源。
若A组（刚性骨架版）性能最好：说明模型主要依赖最强方向进行微调，中间特征值作用有限。
若C组（随机丢弃版）性能最好：说明特征值的位置不重要，保留足够多的奇异值数量才是关键。
若基线模型（原始版）性能最好：说明任何形式的奇异值截断都会损害模型能力，所有特征值都不可或缺。
关键约束
数值稳定性：在SVD后，将奇异值置为0并重构矩阵时，需确保重构后的矩阵不会因数值问题导致梯度爆炸或梯度消失。
谱分布观察：在实验前，先画出模型原始奇异值的分布图。如果模型本身的谱就非常“陡峭”（即大量奇异值趋近于0），那么“中间80%”可能已经包含了大部分能量，这与“松散谱”理论是兼容的。
入侵维度记录：如果采用LoRA进行微调，额外记录微调后模型权重变化的奇异值谱，观察LoRA引入的“入侵维度”主要分布在哪个区间。
实验报告要求
实验结果应清晰报告以下内容：

各模型在测试集上的准确率。
各模型在训练集和测试集上的性能差距（泛化误差）。
对实验结果的分析，验证或反驳核心假设。
这个 AGENTS.md 文件将作为你与AI助手沟通的“合作协议”，确保它能够准确理解并执行这个复杂的实验。你可以直接使用这个文件，或根据你的具体模型和数据集进行微调。